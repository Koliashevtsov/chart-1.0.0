export const labelTextAlign = (points, index) => {
    if (index === 0) {
        return 'left';
    }
    else if (index === points.length - 1) {
        return 'right';
    }
    else {
        return 'center';
    }
};
//# sourceMappingURL=label-text-align.js.map