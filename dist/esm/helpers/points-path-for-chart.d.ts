import { Point, TPointPath } from '../types';
export declare const pointsPathForChart: (name: string | null, data: string[], labels: string[], basePoint: Point, areaHeight: number, step: number, scale: number, absOffsetY: number) => TPointPath[];
