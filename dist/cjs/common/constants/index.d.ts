import { DefOptions } from '../../types';
declare const defaultChartOptions: DefOptions;
declare const defaultGridOpt: {
    yScale: number;
};
declare const defaultSizes: {
    verticalAxisWidth: number;
    horizontalAxisHeight: number;
    nameTabWidth: number;
    nameTabHeight: number;
    valueTabWidth: number;
    valueTabHeight: number;
    labelTabWidth: number;
    labelTabHeight: number;
};
declare const basePoint: {
    pointX: number;
    pointY: number;
};
declare const baseOffset: {
    distanceX: number;
    distanceY: number;
};
declare const baseCursorPoint: {
    pointX: number;
    pointY: number;
};
export { defaultChartOptions, defaultSizes, defaultGridOpt, basePoint, baseOffset, baseCursorPoint };
